#include<stdio.h>
#include<fcntl.h>
int main()
{
int number,process[10],queue[50],flag=0,time_quantum,arrival[],brust_time[50],i,j,k,l;
printf("enter no of processes");
scanf("%d",&number);
for(int i=1;i<=n;i++)
{
printf("Arrival time and cpu time brust for process",i);
scanf("%d",&arrival[i]);
scanf("%d",&brust_time);
}
